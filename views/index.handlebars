<h1> Eat Da-BURGAR! </h1>

<main> 
    <div class= "container">
        <div class ="row">
        <div class = "col s6">
            {{#each burgers}}
            {{#unless this.devoured}}
            <div>
                {{!-- <form action="/burgers{{this.id}}?_method=PUT" method="POST"> --}}
                <div class="row mt-4">
      <div class="col-md-2 col-sm-2">
        <a class="btn btn-lighty"> DEVOUR! </a>
      </div>
                </form>
                </div>
                {{/unless}}
                {{/each}}
            </div>
            
            <div class="col s6">
                {{#each burgers}}
                {{#if this.devoured}}
                <div class='burgerEaten'>
                    <div>
                        {{this.burger_name}}
                    </div>
                </div>
                </div>
            {{/if}}
            {{/each}}

        </div>
        </div>
    </div>
</main>

<h2> Add A Burger Option</h2> 

<div class ="row">
    <div class= "col s12">
        {{!-- <form action="/burgers" method="POST"> --}}
        <input type="text" id="name" name="burger_name" placeholder="NOM NOM MORE MORE">
         <div class="row mt-4">
      <div class="col-md-2 col-sm-2">
        <a class="btn btn-light"> ANOTHA ONE!</a>
      </div>
        </form>
    </div>
</div>